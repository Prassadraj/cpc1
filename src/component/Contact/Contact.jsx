import React from "react";
import Transition from "../Transition/Transition";
function Contact() {
  return (
    <div className="flex justify-center items-center h-[50vh]">Contact</div>
  );
}

export default Transition(Contact);
