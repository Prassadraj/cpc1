import React from "react";
import Transition from "../Transition/Transition";
function About() {
  return <div className="flex justify-center items-center h-[50vh]">About</div>;
}

export default Transition(About);
